<%= form_for @review do |r| %>
	<%= r.text_area :review, :class => 'user_input_textarea' %>
	<%= r.hidden_field :user_id, :value => @user.id %>
	<%= r.hidden_field :place_id, :value => @place.id %>
	<%= r.submit, :class => "circle_button" %>
<% end %>